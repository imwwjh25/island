---
phase: 06-infrastructure-fixes
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - VibeIsland/Sound/SoundManager.swift
autonomous: true
requirements: [FIX-02]
user_setup: []

must_haves:
  truths:
    - "Sound effects play correctly in menu bar application context"
    - "Audio plays even when app is in background (LSUIElement)"
    - "Sound playback respects system mute state"
  artifacts:
    - path: "VibeIsland/Sound/SoundManager.swift"
      provides: "macOS audio session configuration"
      contains: "AVAudioApplication.setCategory"
  key_links:
    - from: "SoundManager.swift"
      to: "AVAudioApplication"
      via: "setCategory(.playback)"
      pattern: "AVAudioApplication.setCategory"
    - from: "SoundManager.playStateChangeSound()"
      to: "NSApplication.shared"
      via: "activate(ignoringOtherApps: true)"
      pattern: "NSApplication.shared.activate"
---

<objective>
配置 macOS AVAudioApplication 音频会话，使音效在菜单栏应用 (LSUIElement) 中正常播放。

Purpose: macOS 菜单栏应用不在 Dock 显示，系统可能不激活其音频会话。正确配置确保音效播放。
Output: 正常工作的音效播放系统
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-infrastructure-fixes/06-RESEARCH.md
@.planning/phases/06-infrastructure-fixes/06-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts executor needs -->

From VibeIsland/Sound/SoundManager.swift (current implementation):
```swift
class SoundManager {
    static let shared = SoundManager()
    private var audioPlayer: AVAudioPlayer?
    @Published var isSoundEnabled: Bool = true
    func playStateChangeSound()
    func isSystemMuted() -> Bool
    private func loadSoundFile()
}
```

From RESEARCH.md - macOS Audio Pattern (lines 107-130):
```swift
// macOS 音频配置
import AVFAudio

func configureAudioSession() {
    #if os(macOS)
    // macOS 使用 AVAudioApplication，不是 AVAudioSession
    try? AVAudioApplication.setCategory(.playback, mode: .default)
    #endif
}

func playSound() {
    // 菜单栏应用可能需要先激活
    #if os(macOS)
    NSApplication.shared.activate(ignoringOtherApps: true)
    #endif
    audioPlayer?.play()
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 添加 macOS 音频会话配置到 SoundManager</name>
  <files>VibeIsland/Sound/SoundManager.swift</files>
  <read_first>
    - /Users/Zhuanz/island/VibeIsland/Sound/SoundManager.swift (当前实现，无音频会话配置)
    - /Users/Zhuanz/island/.planning/phases/06-infrastructure-fixes/06-RESEARCH.md (lines 107-130 推荐模式)
  </read_first>
  <behavior>
    - Test 1: SoundManager.init() calls configureAudioSession() before loadSoundFile()
    - Test 2: configureAudioSession() uses AVAudioApplication.setCategory(.playback, mode: .default)
    - Test 3: Code is platform-specific (#if os(macOS))
  </behavior>
  <action>
    修改 SoundManager.swift，添加 macOS 音频会话配置：

    1. 在文件顶部确认 import AVFAudio（macOS 11+ 使用此模块）：
       ```swift
       import AVFAudio  // 添加此 import（如果尚未存在）
       ```

    2. 在 private init() 中添加音频会话配置调用（在 loadSoundFile() 之前）：
       ```swift
       private init() {
           // 从 UserDefaults 加载音效设置
           loadSoundEnabledSetting()

           // 配置 macOS 音频会话（新增）
           configureAudioSession()

           // 加载音效文件
           loadSoundFile()
       }
       ```

    3. 添加新的 configureAudioSession() 方法（在 loadSoundFile() 之后）：
       ```swift
       // MARK: - macOS 音频会话配置

       /// 配置 macOS 音频会话
       /// 菜单栏应用 (LSUIElement) 需要正确设置音频 category
       private func configureAudioSession() {
           #if os(macOS)
           // macOS 使用 AVAudioApplication，不是 iOS 的 AVAudioSession
           do {
               try AVAudioApplication.setCategory(.playback, mode: .default)
               print("✅ macOS 音频 category 已配置: .playback")
           } catch {
               print("⚠️ 配置音频 category 失败: \(error.localizedDescription)")
           }
           #else
           // iOS 使用 AVAudioSession（虽然此应用不支持 iOS，保留代码完整性）
           do {
               try AVAudioSession.sharedInstance().setCategory(.playback)
           } catch {
               print("⚠️ 配置音频 category 失败: \(error.localizedDescription)")
           }
           #endif
       }
       ```

    参考 RESEARCH.md lines 286-315 的完整实现示例。
  </action>
  <verify>
    <automated>grep -n "configureAudioSession" VibeIsland/Sound/SoundManager.swift && grep -n "AVAudioApplication.setCategory" VibeIsland/Sound/SoundManager.swift</automated>
  </verify>
  <done>
    - SoundManager.swift 包含 "configureAudioSession()" 方法
    - SoundManager.swift 包含 "AVAudioApplication.setCategory(.playback"
    - init() 调用 configureAudioSession() 在 loadSoundFile() 之前
    - 包含 #if os(macOS) 平台条件编译
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 添加应用激活逻辑确保音效播放</name>
  <files>VibeIsland/Sound/SoundManager.swift</files>
  <read_first>
    - /Users/Zhuanz/island/VibeIsland/Sound/SoundManager.swift (当前实现，需修改 playStateChangeSound)
    - /Users/Zhuanz/island/.planning/phases/06-infrastructure-fixes/06-RESEARCH.md (lines 322-329 激活模式)
  </read_first>
  <behavior>
    - Test 1: playStateChangeSound() calls NSApplication.shared.activate(ignoringOtherApps: true) before play
    - Test 2: Activation is platform-specific (#if os(macOS))
    - Test 3: play() is called after activation
  </behavior>
  <action>
    修改 SoundManager.swift 的 playStateChangeSound() 方法，添加应用激活逻辑：

    在 playStateChangeSound() 方法中，在 audioPlayer?.play() 之前添加：

    ```swift
    func playStateChangeSound() {
        // 检查音效是否启用
        guard isSoundEnabled else {
            return
        }

        // 检查系统是否静音
        guard !isSystemMuted() else {
            return
        }

        // 检查频率限制
        if let lastTime = lastPlayTime {
            let timeSinceLastPlay = Date().timeIntervalSince(lastTime)
            guard timeSinceLastPlay >= minPlayInterval else {
                return
            }
        }

        // 检查音效文件是否已加载
        guard soundFileLoaded, let player = audioPlayer else {
            return
        }

        // macOS 菜单栏应用需要激活才能播放音效（新增）
        #if os(macOS)
        NSApplication.shared.activate(ignoringOtherApps: true)
        #endif

        // 重置到开头
        player.currentTime = 0

        // 播放音效
        do {
            try player.play()
            lastPlayTime = Date()
            print("🔊 播放状态变化音效")
        } catch {
            print("⚠️ 播放音效失败: \(error.localizedDescription)")
        }
    }
    ```

    关键修改位置：在 `// 重置到开头` 注释之前添加 NSApplication.shared.activate() 调用。

    参考 RESEARCH.md lines 322-329 的实现模式。
  </action>
  <verify>
    <automated>grep -n "NSApplication.shared.activate" VibeIsland/Sound/SoundManager.swift && grep -A5 "NSApplication.shared.activate" VibeIsland/Sound/SoundManager.swift | grep "player.currentTime"</automated>
  </verify>
  <done>
    - SoundManager.swift playStateChangeSound() 包含 "NSApplication.shared.activate(ignoringOtherApps: true)"
    - 激活调用在 player.currentTime = 0 之前
    - 包含 #if os(macOS) 平台条件编译
    - player.play() 在激活之后调用
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>SoundManager 已配置 macOS AVAudioApplication 音频会话和应用激活逻辑</what-built>
  <how-to-verify>
    1. 运行应用：`xcodebuild -scheme VibeIsland build && run`
    2. 触发状态变化（启动一个 Claude Code 会话）
    3. 确认听到音效播放
    4. 确认系统静音时无音效播放
    5. 检查控制台输出包含 "✅ macOS 音频 category 已配置"
  </how-to-verify>
  <resume-signal>Type "approved" if sound plays correctly, or describe issues</resume-signal>
</task>

</tasks>

<verification>
执行以下验证确认修复完成：

1. **代码验证：**
   - `grep "AVAudioApplication.setCategory" VibeIsland/Sound/SoundManager.swift` 确认 API 使用
   - `grep "NSApplication.shared.activate" VibeIsland/Sound/SoundManager.swift` 确认激活逻辑
   - `grep "#if os(macOS)" VibeIsland/Sound/SoundManager.swift` 确认平台条件编译

2. **构建验证：**
   - `xcodebuild -scheme VibeIsland build` 确认无编译错误

3. **功能验证（手动）：**
   - 启动应用触发状态变化，确认音效播放
   - 系统静音时确认无音效
</verification>

<success_criteria>
1. Sound effects play correctly in menu bar application context (FIX-02)
2. Audio plays even when app is in background (LSUIElement)
3. Sound playback respects system mute state (existing feature maintained)
4. AVAudioApplication.setCategory(.playback) correctly configured for macOS
5. NSApplication.shared.activate(ignoringOtherApps: true) called before playback
6. All code uses platform-specific compilation (#if os(macOS))
</success_criteria>

<output>
After completion, create `.planning/phases/06-infrastructure-fixes/06-02-SUMMARY.md`
</output>