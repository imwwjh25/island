---
phase: 06-infrastructure-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - VibeIsland/MenuBar/VibeIslandMenuBar.swift
  - VibeIsland/MenuBar/CompactStatusView.swift
  - VibeIsland/MenuBar/ExpandedDetailsView.swift
  - VibeIsland/Permissions/AccessibilityManager.swift
  - VibeIsland/Permissions/PermissionPromptView.swift
  - VibeIsland/Window/WindowMonitor.swift
autonomous: true
requirements: [FIX-01, FIX-03]
user_setup: []

must_haves:
  truths:
    - "Singleton state manager persists correctly across view updates without state loss"
    - "App requests Accessibility permission at launch"
    - "User sees clear guidance when Accessibility permission is not granted"
    - "Permission status updates reactively when user changes it in System Settings"
  artifacts:
    - path: "VibeIsland/MenuBar/VibeIslandMenuBar.swift"
      provides: "App entry point with @StateObject and environmentObject injection"
      contains: "@StateObject private var stateManager"
    - path: "VibeIsland/MenuBar/CompactStatusView.swift"
      provides: "Child view receiving state via @EnvironmentObject"
      contains: "@EnvironmentObject var stateManager"
    - path: "VibeIsland/MenuBar/ExpandedDetailsView.swift"
      provides: "Detail view receiving state via @EnvironmentObject"
      contains: "@EnvironmentObject var stateManager"
    - path: "VibeIsland/Permissions/AccessibilityManager.swift"
      provides: "Accessibility permission management singleton"
      contains: "class AccessibilityManager"
    - path: "VibeIsland/Permissions/PermissionPromptView.swift"
      provides: "UI for permission denial guidance"
      contains: "struct PermissionPromptView"
  key_links:
    - from: "VibeIsland/MenuBar/VibeIslandMenuBar.swift"
      to: "StateManager.shared"
      via: "@StateObject + .environmentObject()"
      pattern: "@StateObject.*StateManager.shared"
    - from: "CompactStatusView.swift"
      to: "StateManager"
      via: "@EnvironmentObject"
      pattern: "@EnvironmentObject var stateManager"
    - from: "AccessibilityManager.swift"
      to: "WindowMonitor.swift"
      via: "permission check before window monitoring"
      pattern: "AccessibilityManager.shared.hasPermission"
---

<objective>
修复状态管理 (@ObservedObject -> @StateObject/@EnvironmentObject) 和添加 Accessibility 权限管理系统，确保后续功能正常工作。

Purpose: 状态管理是所有 UI 功能的基础，Accessibility 权限是窗口监控的前提条件。这两个修复必须先完成。
Output: 稳定的状态传递机制 + 完善的权限管理 UI
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-infrastructure-fixes/06-RESEARCH.md

<interfaces>
<!-- Key types and contracts executor needs -->

From VibeIsland/State/StateManager.swift:
```swift
class StateManager: ObservableObject {
    static let shared = StateManager()
    @Published var agentStates: [AgentState] = []
    func updateAgentState(_ agent: AgentState)
    func getAgentCount(for status: AgentStatus) -> Int
    func hasAwaitingApprovalAgents() -> Bool
}
```

From VibeIsland/MenuBar/VibeIslandMenuBar.swift:
```swift
class MenuBarManager: ObservableObject {
    static let shared = MenuBarManager()
    @Published var isPopoverExpanded = false
}
```

From RESEARCH.md - Accessibility pattern:
```swift
class AccessibilityManager: ObservableObject {
    static let shared = AccessibilityManager()
    @Published var hasPermission: Bool = false
    @Published var hasPromptedUser: Bool = false
    func checkPermission(promptUser: Bool)
    func startMonitoring()
    func openAccessibilitySettings()
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 修复状态管理 - App 入口使用 @StateObject 注入单例</name>
  <files>VibeIsland/MenuBar/VibeIslandMenuBar.swift</files>
  <read_first>
    - /Users/Zhuanz/island/VibeIsland/MenuBar/VibeIslandMenuBar.swift (当前实现，line 112 有问题)
    - /Users/Zhuanz/island/VibeIsland/State/StateManager.swift (StateManager 接口)
    - /Users/Zhuanz/island/VibeIsland/MenuBar/ExpandedDetailsView.swift (需要接收注入)
  </read_first>
  <behavior>
    - Test 1: VibeIslandMenuBar uses @StateObject for StateManager.shared (not @ObservedObject)
    - Test 2: VibeIslandMenuBar uses @StateObject for MenuBarManager.shared
    - Test 3: ExpandedDetailsView receives stateManager via .environmentObject()
  </behavior>
  <action>
    修改 VibeIslandMenuBar.swift (App 入口)，将 @ObservedObject 改为 @StateObject，并通过 .environmentObject() 注入给子视图：

    1. 将 line 112 的 `@ObservedObject private var menuBarManager = MenuBarManager.shared` 改为：
       ```swift
       @StateObject private var stateManager = StateManager.shared
       @StateObject private var menuBarManager = MenuBarManager.shared
       ```

    2. 在 body 中给 ExpandedDetailsView 注入环境对象：
       ```swift
       var body: some Scene {
           MenuBarExtra("Vibe Island", systemImage: "sparkles") {
               ExpandedDetailsView()
                   .environmentObject(stateManager)
                   .environmentObject(menuBarManager)
           }
           .menuBarExtraStyle(.window)
       }
       ```

    参考 RESEARCH.md 方案 A (lines 242-258) 的实现模式。
  </action>
  <verify>
    <automated>grep -n "@StateObject.*StateManager.shared" VibeIsland/MenuBar/VibeIslandMenuBar.swift && grep -n ".environmentObject(stateManager)" VibeIsland/MenuBar/VibeIslandMenuBar.swift</automated>
  </verify>
  <done>
    - VibeIslandMenuBar.swift line ~111 包含 "@StateObject private var stateManager = StateManager.shared"
    - VibeIslandMenuBar.swift body 包含 ".environmentObject(stateManager)"
    - VibeIslandMenuBar.swift body 包含 ".environmentObject(menuBarManager)"
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 修复状态管理 - 子视图使用 @EnvironmentObject 接收</name>
  <files>VibeIsland/MenuBar/CompactStatusView.swift, VibeIsland/MenuBar/ExpandedDetailsView.swift</files>
  <read_first>
    - /Users/Zhuanz/island/VibeIsland/MenuBar/CompactStatusView.swift (当前实现，lines 17-20 有问题)
    - /Users/Zhuanz/island/VibeIsland/MenuBar/ExpandedDetailsView.swift (当前实现，lines 16-19 有问题)
    - /Users/Zhuanz/island/VibeIsland/State/StateManager.swift (StateManager 接口)
    - /Users/Zhuanz/island/VibeIsland/MenuBar/VibeIslandMenuBar.swift (已修改的注入方式)
  </read_first>
  <behavior>
    - Test 1: CompactStatusView uses @EnvironmentObject for StateManager (no initialization)
    - Test 2: CompactStatusView uses @EnvironmentObject for MenuBarManager
    - Test 3: ExpandedDetailsView uses @EnvironmentObject for StateManager
    - Test 4: ExpandedDetailsView uses @EnvironmentObject for MenuBarManager
  </behavior>
  <action>
    修改 CompactStatusView.swift 和 ExpandedDetailsView.swift，将 @ObservedObject 初始化改为 @EnvironmentObject 接收：

    CompactStatusView.swift (lines 17-20):
    ```swift
    // 删除这两行：
    // @ObservedObject private var stateManager = StateManager.shared
    // @ObservedObject private var menuBarManager = MenuBarManager.shared

    // 改为：
    @EnvironmentObject var stateManager: StateManager
    @EnvironmentObject var menuBarManager: MenuBarManager
    ```

    ExpandedDetailsView.swift (lines 16-19):
    ```swift
    // 删除这两行：
    // @ObservedObject private var stateManager = StateManager.shared
    // @ObservedObject private var menuBarManager = MenuBarManager.shared

    // 改为：
    @EnvironmentObject var stateManager: StateManager
    @EnvironmentObject var menuBarManager: MenuBarManager
    ```

    参考 RESEARCH.md 方案 B (lines 261-281) 的实现模式。
  </action>
  <verify>
    <automated>grep -n "@EnvironmentObject var stateManager" VibeIsland/MenuBar/CompactStatusView.swift && grep -n "@EnvironmentObject var stateManager" VibeIsland/MenuBar/ExpandedDetailsView.swift</automated>
  </verify>
  <done>
    - CompactStatusView.swift 不包含 "@ObservedObject.*StateManager.shared"
    - CompactStatusView.swift 包含 "@EnvironmentObject var stateManager: StateManager"
    - ExpandedDetailsView.swift 不包含 "@ObservedObject.*StateManager.shared"
    - ExpandedDetailsView.swift 包含 "@EnvironmentObject var stateManager: StateManager"
  </done>
</task>

<task type="auto">
  <name>Task 3: 创建 AccessibilityManager 权限管理单例</name>
  <files>VibeIsland/Permissions/AccessibilityManager.swift</files>
  <read_first>
    - /Users/Zhuanz/island/VibeIsland/Window/WindowMonitor.swift (当前权限检查实现 lines 78-89)
    - /Users/Zhuanz/island/.planning/phases/06-infrastructure-fixes/06-RESEARCH.md (lines 134-167 推荐模式)
  </read_first>
  <action>
    创建新文件 VibeIsland/Permissions/AccessibilityManager.swift，实现权限管理单例：

    ```swift
    //
    //  AccessibilityManager.swift
    //  VibeIsland
    //
    //  Accessibility 权限管理
    //

    import ApplicationServices
    import Combine
    import Cocoa

    /// Accessibility 权限管理器
    /// 负责检查、请求和监控 Accessibility 权限状态
    class AccessibilityManager: ObservableObject {

        // MARK: - 单例

        static let shared = AccessibilityManager()

        // MARK: - 发布属性

        /// 是否已授权
        @Published var hasPermission: Bool = false

        /// 是否已提示过用户
        @Published var hasPromptedUser: Bool = false

        // MARK: - 私有属性

        private var cancellables = Set<AnyCancellable>()

        // MARK: - 初始化

        private init() {
            checkPermission(promptUser: false)
            startMonitoring()
        }

        // MARK: - 权限检查

        /// 检查权限状态
        /// - Parameter promptUser: 是否显示系统授权对话框
        func checkPermission(promptUser: Bool) {
            let options: CFDictionary?
            if promptUser {
                options = [kAXTrustedCheckOptionPrompt.takeUnretainedValue() as String: true] as CFDictionary
            } else {
                options = nil
            }

            hasPermission = AXIsProcessTrustedWithOptions(options)

            if promptUser {
                hasPromptedUser = true
            }

            print("Accessibility 权限状态: \(hasPermission ? "已授权" : "未授权")")
        }

        // MARK: - 权限监控

        /// 启动权限变化监听
        func startMonitoring() {
            DistributedNotificationCenter.default().addObserver(
                forName: NSWorkspace.accessibilityDisplayOptionsDidChangeNotification,
                object: nil,
                queue: .main
            ) { [weak self] notification in
                self?.hasPermission = AXIsProcessTrusted()
                print("权限变化通知: \(notification.name)")
            }
        }

        // MARK: - 系统设置

        /// 打开系统设置中的 Accessibility 页面
        func openAccessibilitySettings() {
            if #available(macOS 13.0, *) {
                let url = URL(string: "x-apple.systempreferences:com.apple.preference.security?Privacy_Accessibility")
                NSWorkspace.shared.open(url!)
            } else {
                let url = URL(string: "x-apple.systempreferences:com.apple.preference.security")
                NSWorkspace.shared.open(url!)
            }
        }
    }
    ```

    确保文件路径正确：VibeIsland/Permissions/AccessibilityManager.swift
  </action>
  <verify>
    <automated>test -f VibeIsland/Permissions/AccessibilityManager.swift && grep -n "class AccessibilityManager" VibeIsland/Permissions/AccessibilityManager.swift && grep -n "AXIsProcessTrustedWithOptions" VibeIsland/Permissions/AccessibilityManager.swift</automated>
  </verify>
  <done>
    - 文件 VibeIsland/Permissions/AccessibilityManager.swift 存在
    - 包含 "class AccessibilityManager: ObservableObject"
    - 包含 "@Published var hasPermission: Bool"
    - 包含 "AXIsProcessTrustedWithOptions" 调用
    - 包含 "DistributedNotificationCenter" 监听
  </done>
</task>

<task type="auto">
  <name>Task 4: 创建 PermissionPromptView 权限提示 UI</name>
  <files>VibeIsland/Permissions/PermissionPromptView.swift</files>
  <read_first>
    - /Users/Zhuanz/island/.planning/phases/06-infrastructure-fixes/06-RESEARCH.md (lines 400-441 UI 示例)
    - /Users/Zhuanz/island/VibeIsland/Permissions/AccessibilityManager.swift (刚创建的 AccessibilityManager)
  </read_first>
  <action>
    创建新文件 VibeIsland/Permissions/PermissionPromptView.swift，实现权限提示 UI：

    ```swift
    //
    //  PermissionPromptView.swift
    //  VibeIsland
    //
    //  Accessibility 权限提示视图
    //

    import SwiftUI

    /// Accessibility 权限提示视图
    /// 当权限未授权时显示引导用户授权的 UI
    struct PermissionPromptView: View {

        // MARK: - 属性

        @ObservedObject var accessibilityManager = AccessibilityManager.shared

        // MARK: - Body

        var body: some View {
            if !accessibilityManager.hasPermission {
                VStack(spacing: 16) {
                    // 警告图标
                    Image(systemName: "exclamationmark.triangle.fill")
                        .foregroundColor(.orange)
                        .font(.system(size: 32))

                    // 标题
                    Text("需要 Accessibility 权限")
                        .font(.headline)

                    // 说明文本
                    Text("Vibe Island 需要监控终端窗口标题以检测 Claude Code 状态。")
                        .font(.body)
                        .multilineTextAlignment(.center)
                        .foregroundColor(.secondary)

                    // 授权按钮
                    Button("授权") {
                        accessibilityManager.checkPermission(promptUser: true)
                    }
                    .buttonStyle(.borderedProminent)

                    // 打开系统设置按钮
                    Button("打开系统设置") {
                        accessibilityManager.openAccessibilitySettings()
                    }
                    .buttonStyle(.bordered)

                    // 设置路径提示
                    Text("设置路径：系统设置 → 隐私与安全性 → 辅助功能")
                        .font(.caption)
                        .foregroundColor(.secondary)
                }
                .padding()
                .background(Color(NSColor.windowBackgroundColor))
                .cornerRadius(12)
                .shadow(radius: 4)
            }
        }
    }

    #Preview {
        PermissionPromptView()
            .previewLayout(.sizeThatFits)
            .padding()
    }
    ```

    确保文件路径正确：VibeIsland/Permissions/PermissionPromptView.swift
  </action>
  <verify>
    <automated>test -f VibeIsland/Permissions/PermissionPromptView.swift && grep -n "struct PermissionPromptView" VibeIsland/Permissions/PermissionPromptView.swift && grep -n "打开系统设置" VibeIsland/Permissions/PermissionPromptView.swift</automated>
  </verify>
  <done>
    - 文件 VibeIsland/Permissions/PermissionPromptView.swift 存在
    - 包含 "struct PermissionPromptView: View"
    - 包含授权按钮和打开系统设置按钮
    - UI 在 !hasPermission 时显示
  </done>
</task>

<task type="auto">
  <name>Task 5: 集成 AccessibilityManager 到 WindowMonitor 并添加权限 UI 到 ExpandedDetailsView</name>
  <files>VibeIsland/Window/WindowMonitor.swift, VibeIsland/MenuBar/ExpandedDetailsView.swift</files>
  <read_first>
    - /Users/Zhuanz/island/VibeIsland/Window/WindowMonitor.swift (当前实现，需替换 checkAccessibilityPermission)
    - /Users/Zhuanz/island/VibeIsland/MenuBar/ExpandedDetailsView.swift (需添加权限提示 UI)
    - /Users/Zhuanz/island/VibeIsland/Permissions/AccessibilityManager.swift (刚创建的 AccessibilityManager)
    - /Users/Zhuanz/island/VibeIsland/Permissions/PermissionPromptView.swift (刚创建的 UI)
  </read_first>
  <action>
    修改 WindowMonitor.swift 和 ExpandedDetailsView.swift 集成 AccessibilityManager：

    1. WindowMonitor.swift - 替换 checkAccessibilityPermission 方法 (lines 78-89)：
       ```swift
       /// 检查 Accessibility 权限
       private func checkAccessibilityPermission() {
           // 使用 AccessibilityManager 检查，不在此处提示
           // UI 层负责显示提示
           let isTrusted = AccessibilityManager.shared.hasPermission

           if !isTrusted {
               logger.warning("⚠️ 需要辅助功能权限才能监控窗口")
               print("⚠️ 需要辅助功能权限才能监控窗口")
           } else {
               logger.info("✅ 已获得辅助功能权限")
               print("✅ 已获得辅助功能权限")
           }
       }
       ```
       同时在文件顶部添加 import（如果需要）和确保在 start() 中调用 AccessibilityManager.shared.checkPermission(promptUser: true)

    2. ExpandedDetailsView.swift - 在空状态视图位置添加 PermissionPromptView：
       在 emptyStateView 的 VStack 开始处添加：
       ```swift
       // 权限提示（优先显示）
       if !AccessibilityManager.shared.hasPermission {
           PermissionPromptView()
               .padding(.bottom, 16)
       }
       ```
       需要添加 @ObservedObject 引用：
       ```swift
       @ObservedObject private var accessibilityManager = AccessibilityManager.shared
       ```

       或使用 @StateObject 来持有引用（推荐）：
       ```swift
       @StateObject private var accessibilityManager = AccessibilityManager.shared
       ```
  </action>
  <verify>
    <automated>grep -n "AccessibilityManager.shared" VibeIsland/Window/WindowMonitor.swift && grep -n "PermissionPromptView" VibeIsland/MenuBar/ExpandedDetailsView.swift</automated>
  </verify>
  <done>
    - WindowMonitor.swift 使用 AccessibilityManager.shared.hasPermission
    - WindowMonitor.swift 在 start() 调用 AccessibilityManager.shared.checkPermission(promptUser: true)
    - ExpandedDetailsView.swift 包含 "@StateObject.*accessibilityManager"
    - ExpandedDetailsView.swift 在空状态区域显示 PermissionPromptView
  </done>
</task>

</tasks>

<verification>
执行以下验证确认修复完成：

1. **状态管理验证：**
   - `grep "@StateObject.*StateManager" VibeIsland/MenuBar/VibeIslandMenuBar.swift` 确认 @StateObject 使用
   - `grep "@EnvironmentObject.*StateManager" VibeIsland/MenuBar/ExpandedDetailsView.swift` 确认子视图接收
   - 构建项目 `xcodebuild -scheme VibeIsland build` 确认无编译错误

2. **Accessibility 权限验证：**
   - `test -f VibeIsland/Permissions/AccessibilityManager.swift` 确认文件存在
   - `grep "AXIsProcessTrustedWithOptions" VibeIsland/Permissions/AccessibilityManager.swift` 确认 API 使用
   - `grep "DistributedNotificationCenter" VibeIsland/Permissions/AccessibilityManager.swift` 确认监听设置

3. **UI 集成验证：**
   - `grep "PermissionPromptView" VibeIsland/MenuBar/ExpandedDetailsView.swift` 确认 UI 显示
</verification>

<success_criteria>
1. Singleton state manager persists correctly across view updates without state loss (FIX-01)
2. App requests Accessibility permission at launch and handles denial gracefully (FIX-03 partial)
3. User sees clear guidance when Accessibility permission is not granted (FIX-03)
4. All modified files compile without errors
5. @StateObject/@EnvironmentObject pattern correctly implemented per RESEARCH.md
</success_criteria>

<output>
After completion, create `.planning/phases/06-infrastructure-fixes/06-01-SUMMARY.md`
</output>